<h1><Eat-Da-Burger></Eat-Da-Burger>!</h1>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <button class="devourBurger" data-id="{{id}}">Devour It!</button>
    {{/unless}}
  {{/each}}
</ul>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <input type="text" id="input" name="name">
  </div>
    <button type="submit">Add Cat</button>
</form>